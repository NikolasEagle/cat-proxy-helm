apiVersion: v1
kind: Service
metadata:
  name: {{ include "cat-proxy.fullname" . }}
  labels:
    {{- include "cat-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cat-proxy.selectorLabels" . | nindent 4 }}
